CREATE TABLE entry (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    kanji TEXT,
    reading TEXT,
    gloss TEXT,
    position TEXT
);

select_all:
SELECT *
FROM entry;

search_kanji:
SELECT *
FROM entry
WHERE kanji is ?;

search_reading:
SELECT *
FROM entry
WHERE reading LIKE ?;
