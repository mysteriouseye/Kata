CREATE TABLE history (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    text TEXT
);

select_all:
SELECT *
FROM history;

select_limit:
SELECT *
FROM history
LIMIT ?;

insert_row:
INSERT INTO history(text)
VALUES (?);

delete_row:
DELETE
FROM history
WHERE id = ?;